//评分功能
$(function(){
	$('.pro_rating ul li a').click(function(){
		var title=$(this).attr('title');
		alert('您的评分是：'+title);
		var cl=$(this).parent().attr('class');
		var lc=cl+"star";
		$(this).parent().parent().removeClass().addClass("rating").addClass(lc);

		return false;
	});
})
